// CloudAlp.PromoLimit.PromoLimit.js
// Load all your starter dependencies in backend for your extension here
// ----------------

define('CloudAlp.PromoLimit.PromoLimit'
	, [
		'CloudAlp.PromoLimit.PromoLimit.ServiceController',
		'Utils',
		'LiveOrder.Model',
		'underscore',
		'SC.Models.Init'
	]
	, function (
		PromoLimitServiceController,
		Utils,
		LiveOrderModel,
		_,
		ModelsInit

	) {
		// var rec= nlapiLoadRecord('salesorder',4360);
		// var promoLen = rec.getLineItemCount('promotions');
		// var getPromo = [];
		// for(var i=0;i<promoLen;i++){
		// 	var id =  rec.getLineItemValue('promotions','promocode',i);
		// 		if(id != undefined || id !=null){
		// 					getPromo.push(id);
		// 		}

		// }
	});